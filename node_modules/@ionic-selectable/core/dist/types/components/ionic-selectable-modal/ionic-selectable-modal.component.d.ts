import { ComponentInterface } from '../../stencil-public-runtime';
/**
 * @internal
 */
export declare class IonicSelectableModalComponent implements ComponentInterface {
  element: HTMLIonicSelectableModalElement;
  private selectableComponent;
  private headerElement;
  private toggleUpdate;
  infiniteScrollElement: HTMLIonInfiniteScrollElement;
  virtualScrollElement: HTMLIonVirtualScrollElement;
  contentElement: HTMLIonContentElement;
  /**
   * Rerender the component
   */
  update(): Promise<void>;
  connectedCallback(): void;
  componentDidLoad(): void;
  private renderItem;
  private renderHeader;
  render(): void;
}
